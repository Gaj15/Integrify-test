
const LoadingIndicator = ({ message }) => {
   return <h3 style={{color: 'black', textAlign: 'center', height: '100%', minHeight: '100%'}}>
     {message}
    </h3>;
};

export default LoadingIndicator;
