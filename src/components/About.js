
const About = () => {
  return <p>
    This is a about page for Integrify job assignment to evaluate the candidate's understanding of react concepts.
    The main reason for using react lies in the ability to create components based on functionality set, i.e. to divide the whole application into small independent parts (components). 
    We use jsonplaceholder API to get the users data and dispaly it in a meaningful way.
  </p>
};

export default About;